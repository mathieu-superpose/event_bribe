<p id="notice"><%= notice %></p>



<!-- <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Start date</th>
      <th>Duration</th>
      <th>Price</th>
      <th>Location</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.description %></td>
        <td><%= event.start_date %></td>
        <td><%= event.duration %></td>
        <td><%= event.price %></td>
        <td><%= event.location %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %> -->

<div class="jumbotron" style="background-image: url(https://images.pexels.com/photos/3781895/pexels-photo-3781895.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260); background-size: 80%; background-repeat: no-repeat; background-position: center;">
        <div class="container">
          <h1 class="display-3"><strong>Le futur est dans l'enveloppe</strong></h1>
          <p>Prenez votre part du gateau!<br>Vous aussi faites du bénef sans trop d'effort.</p>

          <p><a class="btn btn-secondary btn-lg" href="<%= new_event_path %>" role="button">Publiez votre évenement &raquo;</a></p>
        </div>
      </div>

      <div class="container">

        <hr>

        <% Event.all.each do |event| %>
        <div class="col-md-4">
          <h2><% event.title %></h2>
          <p><% event.description %></p>
        </div>
        <% end %>
<div class="row mb-3">
        <% @events.each do |event| %>
      
      <div class="col-md-4">
        
        <h2><%= event.title %></h2>
        <p><%= event.start_date %></p>
        <p><%= event.description %></p>
        <p><a class="btn btn-secondary small" href="<%= event_path(event) %>" role="button">View details &raquo;</a></p>
        
      
    </div>
      
    <% end %>
</div>

      </div> <!-- /container -->

    </main>

    <footer class="container">
      <p>&copy; Eventbribe 2020 - THP s.13</p>
    </footer>



